$(document).ready(function () {
	// Save the user's login credentials to local storage
	// Not safe, but its the simplest solution for now (before implementing authentication cookies)
	$('#login').bind('submit', function () {
		
	});
	
});